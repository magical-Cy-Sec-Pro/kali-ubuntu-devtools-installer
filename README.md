# Kali & Ubuntu DevTools Installer

Automated scripts to install essential developer, networking, and container tools on **Kali Linux** and **Ubuntu**.

## 🔧 Tools Installed

- Docker
- Git
- net-tools (`ifconfig`)
- curl & wget
- Python 3 + pip
- build-essential (C compiler, make, etc.)
- unzip & vim

## 📜 Usage

### 1. Clone the Repository

git clone https://github.com/<your-username>/kali-ubuntu-devtools-installer.git
cd kali-ubuntu-devtools-installer


# kali-ubuntu-devtools-installer
Docker  Git  ifconfig (via net-tools)  Python  Other utilities (like curl, wget, etc.)

#How to use files

chmod +x kali-devtools-install.sh
./kali-devtools-install.sh

Use sudo if your user isn’t privileged already.

